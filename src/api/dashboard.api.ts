import { apiFetch } from './client';

export const dashboardApi = {
  // We can fetch data concurrently here or leave it to the hook.
  // We will re-use existing endpoints from other APIs for the dashboard in the hook.
  // If we needed specific aggregate endpoints, they would be here.
};
